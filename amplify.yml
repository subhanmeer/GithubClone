version: 1
applications:
  - appRoot: frontend
    frontend:
      phases:
        preBuild:
          commands:
            - node -v
            - npm -v
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: dist   # Change to 'build' if you’re using create-react-app
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
